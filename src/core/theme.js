import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#3B018E',
    secondary: '#05DAC3',
    error: '#f13a59',
    backg: 'white',
  }
};